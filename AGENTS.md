# AI Agent Instructions

This workspace contains a single JavaScript file: `test.js`.

## What to know

- The project is extremely small and currently has no package.json, build system, or tests.
- `test.js` exports a simple `add(x, y)` function and logs the result of `add(1, 2)`.
- There are no existing conventions, scripts, or documentation files in this repository.

## How to contribute

- Keep changes minimal and clearly focused on adding project structure or functionality.
- If you add dependencies, also add a `package.json` and include installation instructions.
- If you add tests, include a test runner configuration and a short note in `AGENTS.md`.

## Notes for AI agents

- Do not assume this is a complete application; it is currently a single utility script.
- Prefer asking the user before introducing significant project scaffolding.
- 已为当前工作区添加一个简单的 Vue 3 + NestJS monorepo 示例。
